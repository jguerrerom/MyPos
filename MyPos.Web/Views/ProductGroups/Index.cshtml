@model IEnumerable<MyPos.Web.Data.Entities.ProductGroup>

@{
    ViewData["Title"] = "Index";
}

<!-- Content Header (Page header) -->
<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1>Manage Product Groups</h1>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="#">Home</a></li>
                    <li class="breadcrumb-item active">Product Groups</li>
                </ol>
            </div>
        </div>
    </div><!-- /.container-fluid -->
</section>

<section class="content">


    <!-- Default box -->
    <div class="card">
        <div class="card-header">
            <h3 class="card-title"></h3>

            <a class="btn btn-info btn-sm" asp-action="Create">Create New</a>
            <hr />
            @using (Html.BeginForm())
            {
                @*<p class="text-right">
                    Find: @Html.TextBox("SearchString")
                    <input class="btn btn-info btn-sm" type="submit" value="Search" />
                </p>*@

        <div class="form-group row text-right">
            @*<label for="SearchString" class="col-sm-2 col-form-label">Find </label>
            <div class="col-sm-10 row text-right">
                <input type="text" class="form-control" id="SearchString" placeholder="Search String">
                <input class="btn btn-info btn-sm" id="submit" type="submit" value="Search" />
            </div>*@

            <div class="col-sm-12 row text-right">
                <div class="col-sm-6 input-group input-group-sm text-right">
                    <input class="col-sm-6 form-control form-control-navbar" name="SearchString" type="search" placeholder="Enter the text to search" aria-label="Enter the text to search">
                    <div class="input-group-append">
                        @*<button class="btn btn-navbar" id="submit" type="submit">
            <i class="fas fa-search"></i>
        </button>*@
                        <input class="btn btn-info btn-sm" id="submit" type="submit" value="Search" />
                    </div>
                </div>
            </div>
        </div>
            }
        </div>
        <div class="card-body p-0">
            <table class="table table-striped projects">
                <thead>
                    <tr>
                        <th>
                            @*@Html.DisplayNameFor(model => model.Code)*@
                            @Html.ActionLink("Code", "Index", new { sortOrder = ViewBag.CodeSortParm })
                        </th>
                        <th>
                            @*@Html.DisplayNameFor(model => model.Description)*@
                            @Html.ActionLink("Description", "Index", new { sortOrder = ViewBag.DescriptionSortParm })
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Code)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Description)
                            </td>
                            <td class="project-actions text-right">
                                <a class="btn btn-info btn-sm" asp-action="Edit" asp-route-id="@item.Id"><i class="fas fa-pencil-alt"></i> Edit</a>
                                <a class="btn btn-primary btn-sm" asp-action="Details" asp-route-id="@item.Id"><i class="fas fa-folder"></i> Details</a>
                                <a class="btn btn-danger btn-sm" asp-action="Delete" asp-route-id="@item.Id"><i class="fas fa-trash"></i> Delete</a>
                                
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
        <!-- /.card-body -->
    </div>
    <!-- /.card -->

</section>
<!-- /.content -->